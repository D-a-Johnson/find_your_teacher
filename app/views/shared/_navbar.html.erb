<% if user_signed_in? %>
  <div class="nav-bar text-light d-flex bg-dark justify-content-between align-items-center pt-3 pb-2 px-4">
    <h1>FIND YOUR TEACHER</h1>
    <div class="d-flex left">
      <i class="fas fa-search mt-2"></i>
      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
       <li class="nav-item dropdown">
        <%= cl_image_tag (current_user.avatar), class: "avatar dropdown-toggle", "id": "navbarDropdown", "data-toggle": "dropdown", "aria-haspopup": "true", "aria-expanded": "false" %>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
        <a class="dropdown-item" href="#">Profile</a>
        <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
      </li>
    </div>
  </div>
<% else %>
  <div class="nav-bar text-light d-flex bg-dark justify-content-between align-items-center pt-3 pb-2 px-4">
    <h1>FIND YOUR TEACHER</h1>
    <div class="d-flex">
      <h6 id="sign-up-rounded"><%= link_to "SIGN UP",  new_user_registration_path  %></h6>
      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
      <h6><%= link_to "LOGIN", new_user_session_path %></h6>
    </div>
  </div>
<% end %>
